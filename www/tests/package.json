{
  "name": "www-webdriver-tests",
  "version": "1.0.0",
  "description": "WebDriver tests for the UI",
  "scripts": {
    "test": "wdio run wdio.conf.js && npm run report",
    "test:headless": "HEADLESS=true wdio run wdio.conf.js && npm run report",
    "test:spec": "wdio run wdio.conf.js --spec",
    "test:watch": "wdio run wdio.conf.js --watch",
    "test:only": "wdio run wdio.conf.js",
    "report": "node generate-html-report.js"
  },
  "devDependencies": {
    "@wdio/cli": "^8.40.0",
    "@wdio/junit-reporter": "^8.43.0",
    "@wdio/local-runner": "^8.40.0",
    "@wdio/mocha-framework": "^8.40.0",
    "@wdio/spec-reporter": "^8.40.0",
    "chai": "^4.3.10",
    "chromedriver": "^144.0.0",
    "mocha": "^10.7.3",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^4.3.0",
    "mochawesome-report-generator": "^6.2.0",
    "wdio-chromedriver-service": "^8.1.1"
  }
}
